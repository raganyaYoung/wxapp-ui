/**app.wxss**/
/*.container {
  height: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: space-between;
  padding: 200rpx 0;
  box-sizing: border-box;
} */

@import 'utils/common.wxss';
@import 'utils/util.wxss';

/* 已在app.json配置过window的background-color为#f5f5f5，
    在手机页面显示正常，但在开发者工具不显示，默认为白色
    所以这里为方便在开发者工具中调试，再次设置page的background-color
 */
page {
  background: #f5f5f5;
}